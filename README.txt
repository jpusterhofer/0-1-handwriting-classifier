To run:
Place data and nn.py in the same directory
Run python3 nn.py

This program will train a neural net to identify handwritten zeros and ones from the MNIST training set. Then it will test the model on different a different data set and print the percentage of images that it classified correctly.